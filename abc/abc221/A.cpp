#include <bits/stdc++.h>
using namespace std;

int main() {
	/*
	string s, t;
	cin >> s >> t;
	int n = s.size();
	vector<char> x(n), y(n);
	for(int i = 0;i < n;i++)
	{
		x[i] = s[i];
	}
	for(int i = 0;i < n;i++)
	{
		y[i] = t[i];
	}
	bool flag = false;
	if(s == t)
	{
		flag = true;
	}
	for(int i = 0;i < n - 1;i++)
	{
		vector<char> z(n);
		for(int j = 0;j < n;j++)
		{
			z[j] = x[j];
		}
		swap(z[i], z[i+1]);
		if(y == z)
		{
			flag = true;
		}
	}
	if(flag)
	{
		cout << "Yes" << endl;
	}
	else{
		cout << "No" << endl;
	}
	*/
}