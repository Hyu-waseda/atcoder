#include <bits/stdc++.h>
using namespace std;

int main() {
	int n, k;
	cin >> n >> k;
	vector<int> a(n);
	for(int i = 0;i < k;i++){
		cin >> a[i];
	}
	vector<int> x(n), y(n);
	for(int i = 0;i < n;i++){
		cin >> x[i] >> y[i];
	}
	
}
